const API_BASE = "http://127.0.0.1:9008/api/";

const API_URL = {
  batiments: `${API_BASE}batiments`,
  appartements: `${API_BASE}appartements`,
  interventions: `${API_BASE}interventions`,
  contrats: `${API_BASE}contrats`,
  garants: `${API_BASE}garants`,
  locataires: `${API_BASE}locataires`,
  paiements: `${API_BASE}paiements`,
};

export default API_URL;
